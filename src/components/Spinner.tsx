import React from "react";
import { Spinner as SpinnerStyled, SpinnerWrap } from "../styles/StyledBlocks";

const Spinner: React.FC = () => (
  <SpinnerWrap>
    <SpinnerStyled />
  </SpinnerWrap>
);

export default Spinner;
